/*
abstract class SDKDataState<T> {

  final T ? data;
  final T ? error;

  const SDKDataState({
    this.data,
    this.error
  });
}

class SDKDataSuccess<T> extends SDKDataState<T> {
  const SDKDataSuccess(T data) : super(data: data);
}

class SDKDataFailed<T> extends SDKDataState<T> {
  const SDKDataFailed(error) : super(error: error);
}
*/
