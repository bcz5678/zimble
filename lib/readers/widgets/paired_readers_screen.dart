import 'package:flutter/material.dart';

class PairedReadersScreen extends StatelessWidget {
  const PairedReadersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Paired Readers');
  }
}
