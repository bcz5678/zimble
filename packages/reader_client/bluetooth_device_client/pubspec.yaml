name: bluetooth_device_client
description: Bluetooth Devices client scanning/connecting
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  rxdart: ^0.28.0

  #custom packages
  reader_client:
    path: ../reader_client
  native_channels_client:
    path: ../../native_channels_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
